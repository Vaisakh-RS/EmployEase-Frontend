const UpdateEmployee = ({Close}) => {
    return ( 
        <div>
            <h1>Update Employee</h1>
            <button onClick={Close}>Close</button>
        </div>
     );
}
 
export default UpdateEmployee;
